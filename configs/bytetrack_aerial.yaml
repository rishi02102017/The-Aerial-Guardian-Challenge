# ByteTrack tuned for aerial small targets + camera motion
tracker_type: bytetrack
track_high_thresh: 0.25
track_low_thresh: 0.1
new_track_thresh: 0.25
track_buffer: 45
match_thresh: 0.8
fuse_score: true
# gmc (global motion compensation) — helps drone ego-motion
gmc_method: sparseOptFlow
